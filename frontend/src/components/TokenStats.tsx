// Token stats are no longer available — this component is kept as a stub
// to avoid breaking any future imports while the token system is removed.

interface TokenStatsProps {
  compact?: boolean;
}

export default function TokenStats(_props: TokenStatsProps) {
  return null;
}
