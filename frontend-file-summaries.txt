{
  "frontend/src/index.css": "Global IPGT theme with OKLCH Black/Gold/White color system — all navy/blue tokens replaced with deep black (oklch 0.08), gold primary accent (oklch 0.78 0.15 85), white foreground, Playfair Display serif headings, Inter body font, prose-content utility classes, and print media query styles.",
  "frontend/tailwind.config.js": "Extended Tailwind config with IPGT Black/Gold/White palette — navy and charcoal tokens removed and replaced with black tokens (oklch 0.08–0.20), gold color scale preserved, Playfair Display serif and Inter sans-serif fonts, and custom gold glow shadow utilities.",
  "frontend/index.html": "HTML entry point with IPGT page title and favicon pointing to the generated logo asset.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend integration — useRegisterIP mutation works with anonymous actors (no auth dependency), plus hooks for getAllIPs, searchByTitle, searchByTitleOrHash, filterByCategory, filterByJurisdiction, and user profile operations.",
  "frontend/src/App.tsx": "Main TanStack Router app with routes for Home, Register IP, IP Database, and Whitepaper — Layout is used directly as the root route component (which renders `<Outlet />` internally) without passing children.",
  "frontend/src/components/Layout.tsx": "Layout wrapper composing Header, page Outlet, Footer, and the ProfileSetupModal overlay so the profile setup prompt appears globally on first login.",
  "frontend/src/components/Header.tsx": "Sticky navigation header with IPGT logo, nav links, and a Sign In / Sign Out auth button in both desktop and mobile layouts — uses useInternetIdentity and clears React Query cache on logout.",
  "frontend/src/components/Footer.tsx": "Site footer with deep black background, IPGT branding, nav links, platform feature list (all WIPO references removed), dynamic copyright year, and caffeine.ai attribution with gold accent colors.",
  "frontend/src/components/ProfileSetupModal.tsx": "First-login profile setup modal that appears over all pages when a user is authenticated but has no profile yet — collects display name (required), organisation (optional), and email (optional), with a gold submit button and non-dismissible design.",
  "frontend/src/components/IPCard.tsx": "Reusable clickable IP record card with deep black background, gold borders on hover, gold category badges, and a SHA-256 hash row — no WIPO references.",
  "frontend/src/components/IPDetailModal.tsx": "Full-detail dialog for a selected IP record with deep black modal background, gold accent borders, metadata grid, document hash sections with copy-to-clipboard, and on-chain confirmation banner — no WIPO references.",
  "frontend/src/components/RegistrationSuccessModal.tsx": "Success dialog after IP registration with deep black background, gold success icon and accents, IP ID display, ICP on-chain confirmation, and next-steps list — no WIPO references.",
  "frontend/src/components/TokenStats.tsx": "Stub component that renders nothing; the token stats functionality has been removed along with the backend token system, so all hook imports and UI have been stripped to prevent TypeScript build errors.",
  "frontend/src/pages/Home.tsx": "IPGT landing page with hero section that now shows two matching gold-bordered glass panel CTAs (Register IP above Search Database) replacing the old inline button row, plus all existing features/how-it-works/categories/CTA sections unchanged.",
  "frontend/src/pages/RegisterIP.tsx": "IP registration form page with auth gate — unauthenticated users see a centered sign-in prompt with a login button and a link to the database; authenticated users see the full SHA-256 hashing and file upload registration form.",
  "frontend/src/pages/IPDatabase.tsx": "Global IP Database page with search/filter controls and IPCard grid — fixes the missing `open` prop on `IPDetailModal` by passing `open={selectedRecord !== null}`, and uses the gold-bordered glass panel treatment for the search/filter container.",
  "frontend/src/pages/CoinDashboard.tsx": "Deprecated empty page component that renders nothing, replacing the former IPGT token dashboard.",
  "frontend/src/pages/Whitepaper.tsx": "12-section technical whitepaper with deep black background, gold-accented sticky sidebar TOC, and print-to-PDF button — all WIPO references fully removed and replaced with generic international IP registry language.",
  "frontend/src/utils/urlParams.ts": "Minimal URL parameter utility that exports `getSecretParameter` as a no-op returning `null`, removing all admin token gate logic while preserving the export signature required by `useActor.ts`.",
  "frontend/src/components/AnimatedWorldMap.tsx": "Crystal-clear 3D rotating Earth globe using React Three Fiber — renders only the globe mesh with high-quality texture (anisotropic filtering, 128-segment sphere), a subtle gold atmosphere glow, and a starfield background. All grid lines, city dot markers, and arc connections have been removed for a clean, sharp appearance."
}